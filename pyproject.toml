[project]
name = "hotel-aiemail"
version = "0.1.0"
description = "AI email agent for hotel guest emails"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.52.0",
    "pydantic>=2.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
